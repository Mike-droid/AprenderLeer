package com.itpn.aprenderleer

import android.content.Intent
import android.media.MediaPlayer
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.view.View

class Letras : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_letras)
    }

    fun abrirA(v: View){
        MediaPlayer.create(this,R.raw.a).start()
    }
    fun abrirB(v: View){
        MediaPlayer.create(this,R.raw.b).start()
    }
    fun abrirC(v: View){
        MediaPlayer.create(this,R.raw.c).start()
    }
    fun abrirD(v: View){
        MediaPlayer.create(this,R.raw.d).start()
    }
    fun abrirE(v: View){
        MediaPlayer.create(this,R.raw.e).start()
    }
    fun abrirF(v: View){
        MediaPlayer.create(this,R.raw.f).start()
    }
    fun abrirG(v: View){
        MediaPlayer.create(this,R.raw.g).start()
    }
    fun abrirH(v: View){
        MediaPlayer.create(this,R.raw.h).start()
    }
    fun abrirI(v: View){
        MediaPlayer.create(this,R.raw.i).start()
    }
    fun abrirJ(v: View){
        MediaPlayer.create(this,R.raw.j).start()
    }
    fun abrirK(v: View){
        MediaPlayer.create(this,R.raw.k).start()
    }
    fun abrirL(v: View){
        MediaPlayer.create(this,R.raw.l).start()
    }
    fun abrirM(v: View){
        MediaPlayer.create(this,R.raw.m).start()
    }
    fun abrirN(v: View){
        MediaPlayer.create(this,R.raw.n).start()
    }
    fun abrirÑ(v: View){
        MediaPlayer.create(this,R.raw.enie).start()
    }
    fun abrirO(v: View){
        MediaPlayer.create(this,R.raw.o).start()
    }
    fun abrirP(v: View){
        MediaPlayer.create(this,R.raw.p).start()
    }
    fun abrirQ(v: View){
        MediaPlayer.create(this,R.raw.q).start()
    }
    fun abrirR(v: View){
        MediaPlayer.create(this,R.raw.r).start()
    }
    fun abrirS(v: View){
        MediaPlayer.create(this,R.raw.s).start()
    }
    fun abrirT(v: View){
        MediaPlayer.create(this,R.raw.t).start()
    }
    fun abrirU(v: View){
        MediaPlayer.create(this,R.raw.u).start()
    }
    fun abrirV(v: View){
        MediaPlayer.create(this,R.raw.v).start()
    }
    fun abrirW(v: View){
        MediaPlayer.create(this,R.raw.w).start()
    }
    fun abrirX(v: View){
        MediaPlayer.create(this,R.raw.x).start()
    }
    fun abrirY(v: View){
        MediaPlayer.create(this,R.raw.y).start()
    }
    fun abrirZ(v: View){
        MediaPlayer.create(this,R.raw.z).start()
    }
}